核心挑战是 在==有限的上下文窗口==精准召回与==用户当前意图==相关的历史信息

时间推移 
三维过滤机制 
- 时间边界 SQLite ，会话密度 动态调整窗口
- 专业边界记忆 基于记忆条目关联的Agent 向量->标签 一致
- 安全边界 token校验 敏感字段 脱敏处理 私有信息

交集 先时间过滤再相似度检索，标签和权限 避免全量 如果空，降级为时间+安全边界召回 提示补充

基于重要性评分的分层存储策略
打分模型（交互频次，情感千古，用户显式确认 0-1）最后访问时间
后台 凌晨超过7天 重要性阈值 热存储->温存储
摘要 LLM 生成

超过30天且重要性极低->压缩为冷存储，归档
时间衰减 降低旧记忆召回权重

重评分 相关->提升重要性迁回热存储
通过事务保证原子性，失败重试/日志
摘要锚点，恢复上下文
还原细节