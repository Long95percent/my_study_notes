
- 打算本地使用，但是多次尝试无果，它强制联网安装
     - 好的，关梯子，联网运行
     - 再开梯子，接着问GPT
     - maybe paddleocr必须联网安装？
     - 算了，好歹能用
- ocr识别错误（明明是中文聊天记录却提取出无意义的英文字母）
     - 新版本问题
         - 错误原因：使用第五版本所以只能识别英文＋数字
         - 好呗，那用旧版本
             - 又报错，原来是依赖版本不对，需要numpy降低版本
                 - 如果python版本过高又会不兼容这个旧的numpy版本，还好我用的是python3.10
              - 好的，报错，又来一个oneDNN(WIN和CPU兼容性问题)
                 - 用代码关闭它，继续报错
                 - 原来是用于关闭的那两行代码位置不对，修改，运行，继续报错
                 - 原来是没有彻底禁用，修改，接着报错ValueError，说2.7以上版本不支持其中某个参数
                 - 好的，又重新改了代码，失败，还是强制调用加速，我放弃了，最后打算在导师电脑上跑
总结：什么都干了但是看起来啥都没干






计算机世界是很明确的，说一不二的，但是它明明确确地放着一地坑让你踩
